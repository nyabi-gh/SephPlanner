<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <!-- Unity Mono에서 로드할 수 있는 대상 프레임워크를 유지한다. -->
    <TargetFramework>netstandard2.1</TargetFramework>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <AssemblyName>SephPlanner.Core</AssemblyName>
    <RootNamespace>SephPlanner.Core</RootNamespace>
  </PropertyGroup>
</Project>
