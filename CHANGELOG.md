# 변경 기록

## 0.3.8

**추천이 훨씬 빨리 뜹니다.** 가방 정리 배치를 먼저 보여 주고, 무엇을 집고 무엇을 뺄지에 대한
조언은 곧이어 채웁니다. 기다리는 시간이 줄고, 계산이 끝나지 않아 답이 안 나오던 상황이 없어집니다.
아티팩트별로 레벨 제한을 걸 수 있는 기능도 들어갔습니다.

### Added

- **아티팩트마다 레벨 제한을 걸 수 있습니다(F2 빌드 창).** 줄의 "레벨 제한"을 누르면 1, 2, 3… 으로 올라가고 우클릭으로 되돌립니다. 제한을 걸면 그 아티팩트는 그 레벨까지만 값으로 쳐서, **남는 높은 칸이 다른 아티팩트에게 돌아갑니다.** "이건 3레벨이면 충분하니 나머지는 다른 데 쓰고 싶다"를 별(★)로는 말할 수 없어서 넣었습니다(별은 누가 좋은 칸을 먼저 갖느냐이지, 얼마나 높은 칸이냐가 아닙니다). 막는 것이 아니라 값을 안 쳐 주는 것이라, 그 칸을 아무도 원하지 않으면 여전히 거기 놓일 수 있습니다. 별과 마찬가지로 화면의 점수도 이 기준으로 바뀝니다.
- F10 진단 자료에 계산이 얼마나 걸렸는지가 함께 적힙니다. 느리다는 제보를 받았을 때 짐작 대신 숫자로 답하기 위한 것이고, 평소 플레이에는 영향이 없습니다.

### Changed

- **배치가 먼저 뜹니다.** 전에는 세피라이트 보상 창을 열었다 닫기만 해도 계산이 처음부터 다시 돌아서, 그런 층에서는 "갱신 중"만 계속 보이고 답이 나오지 않을 때가 있었습니다. 이제 후보나 합성기, 소지금만 달라지면 조언만 다시 계산하고 배치는 그대로 둡니다. 추천 내용은 전과 같습니다.
- **F8로 자동 배치를 한 뒤 결과가 곧바로 뜹니다.** 전에는 방금 정리한 가방을 처음부터 다시 계산하느라 몇 초씩 걸렸습니다.
- **석판 합성 추천은 합성기에 가까이 갔을 때 계산합니다.** 창을 열면 이미 준비돼 있고, 멀리 있을 때 괜히 계산하지 않습니다. 멀어서 아직 계산하지 않았을 때는 화면이 그렇다고 알려 줍니다.
- 계산하는 동안에도 직전 추천을 그대로 볼 수 있습니다. 화면을 "계산 중"으로 덮지 않습니다.
- 계산이 전반적으로 빨라지고 메모리를 절반쯤 덜 씁니다. 게임이 이따금 멈칫하던 것도 그만큼 줄어듭니다.
- 적을 잡아 떨어진 주머니가 후보에 뜨는 것이 최대 1초 빨라집니다.

### Fixed

- **시나리오 동행 증표를 들고 있으면 자동 배치(F8)가 아무것도 못 하던 문제를 고쳤습니다.** 옮길 수 없는 그 아이템 때문에 첫 걸음에서 멈추고 "인벤토리가 바뀌었다"는 엉뚱한 안내가 떴습니다. 이제 그 아이템은 지금 칸에 두고 나머지를 그 주위로 배치합니다.
- **F10 진단 메모를 적다가 스페이스를 누르면 그대로 전송되던 문제를 고쳤습니다.** 이제 스페이스는 띄어쓰기이고, 보내는 것은 Enter와 보내기 단추뿐입니다.
- 합성기 창을 열었는데 추천을 아직 계산 중이면 "계산 중"이라고 알려 줍니다. 전에는 "추천 조합 없음"이라고 해서 없는 것처럼 보였습니다.
- 상점을 열었을 때 석판 합성 추천이 같이 따라붙던 문제를 고쳤습니다.
- 북향의 침이 별(★)을 낮춰 둔 아티팩트를 우선해서 강화하던 문제를 고쳤습니다. 별을 주지 않은 아티팩트에서는 전과 같습니다.

## 0.3.7

### Changed

- 아티팩트 점수의 눈금을 다시 맞췄습니다. 능력치를 여럿 함께 올려 주는 아티팩트를 그 능력치 수만큼 중복해서 세고 있어서, 잰 값으로 매긴 점수가 레어도로 어림한 점수보다 한 번 부풀어 있었습니다. **311종 전부의 점수가 움직입니다.** 배치를 고르는 기준은 그대로이고, 점수끼리 견주는 자는 이제 하나입니다.
- 콤보 한 단계의 값어치(3.4 → 2.59)와 전체 피해 보너스 1점의 값어치(0.4 → 0.26)를 같은 눈금으로 다시 쟀습니다. 조화의 수정처럼 이웃에 달린 아티팩트의 자리 가치가 함께 움직입니다.
- 0.3.6이 넣은 점수 비교 눈금도 척도를 따라 0.1레벨에서 0.05레벨로 줄였습니다. 그대로 두면 실제 레벨 한 칸의 차이를 동점으로 삼키는 아티팩트가 생깁니다.
- 콤보 한 단계의 값어치, 전체 피해 보너스 환산값, 점수 비교 눈금을 카탈로그를 지을 때 직접 잽니다. 전에는 셋 다 코드에 박혀 있어서 게임이 균형을 고치면 아무 소리 없이 낡았습니다.
- 그 능력치를 주는 아티팩트가 자기뿐이라 환산율이 자기 자신에서 나온 경우, 칸 설명이 그렇다고 알려 줍니다. 잰 값이라는 것만 말하면 짐작에 가까운 숫자가 실측과 같은 무게로 보입니다.
- 능력치 표를 쓰지 않고 자기 코드로 능력치를 올려 주는 아티팩트도 값어치를 잽니다. 부서진 나무뿌리의 최대 MP, 검술 교본의 공격 속도처럼 화면에는 숫자가 뜨는데 우리는 레어도로만 어림잡던 것들입니다. 게임 코드를 직접 읽으므로 게임이 바뀌면 따라갑니다.
- 석판·아티팩트 데이터를 한 번 다시 모읍니다. 게임을 켜면 저절로 이루어지며 그동안 점수가 늦게 뜰 수 있습니다.

### Fixed

- 연동 무기를 들고 있지 않아 꺼져 있는 아티팩트도 강화 우선(★)을 주면 그 자리를 받습니다. 전에는 꺼진 아티팩트의 값어치가 0이라 별을 셋 줘도 남는 칸으로 밀렸습니다. 무기를 바꾸면 그대로 켜지며, 점수는 지금 실제로 받는 것만 세므로 그때까지 오르지 않습니다. 계획 경고와 칸 설명이 그 사실을 알려 줍니다. 견고 계열 39종 중 22종이 무기 연동이라 그쪽을 쓰면 자주 만납니다.
- 영원의 식이 가방 반대편으로 옮겨지던 문제를 수정했습니다. 대립의 천칭과 똑같이 왼편·오른편에 따라 주는 것이 달라지는데 그쪽만 자리를 지키고 있었습니다. 이제 둘 다 지금 놓인 쪽을 유지하며, 칸 설명도 그렇게 알려 줍니다.
- 붙박이로 큰 능력치를 주는 아티팩트의 값어치가 터지던 문제를 수정했습니다. 혈석 귀걸이가 값어치 27레벨로 격자에서 가장 값진 아티팩트가 되어 있었습니다. 레벨을 올려도 거의 안 오르는 능력치를 "레벨당 얼마"로 읽은 것이 원인이며, 붙박이 보너스는 레벨로 사는 것이 아니라 켜 두는 값입니다.
- 토끼마을 경비병 투구처럼 레벨을 올리면 회피가 더 깎이는 아티팩트가 낮은 레벨 칸에 고정되던 문제를 수정했습니다. 회피·이동 속도 같은 능력치의 환산율이 너무 낮게 잡혀 있어서 손해 쪽이 과장돼 있었습니다. 도마뱀 판금 갑옷은 여전히 레벨을 올리면 손해가 커지는데, 그쪽은 환산율이 아니라 실제 맞교환입니다(0.3.6이 넣은 칸 설명이 그 이유를 알려 줍니다).

## 0.3.6

### Added

- 레벨을 올리면 오히려 손해가 커지는 아티팩트는 칸 설명이 그 이유를 알려 줍니다. 높은 칸을 비워 둔 채 낮은 칸에 두는 것이 실수처럼 보이던 자리입니다.

### Changed

- 강화 우선(★)을 지정한 아티팩트의 점수가 전보다 낮게 보일 수 있습니다. 점수가 부풀어 있었던 것이며 배치를 비교하는 기준은 그대로입니다.
- 점수 차이가 아주 작을 때는 감점이 있는 빈칸을 줄이고 지금 자리를 지키는 쪽을 고릅니다. 점수는 능력치를 환산해 어림한 값이므로 그 어림의 오차보다 작은 차이로는 배치를 바꾸지 않습니다.

### Fixed

- 강화 우선(★)을 지정한 아티팩트가 오히려 낮은 레벨 칸에 머물던 문제를 수정했습니다. 능력치 밖에 따로 효과가 있는 아티팩트에서 손해가 점수를 올리는 쪽으로 뒤집혀 있었고, 실드 메이트처럼 레벨이 오르며 손해가 줄어드는 아티팩트가 여기 해당했습니다.

## 0.3.5

### Added

- 1.0.31이 들여온 성장 아티팩트를 얼마나 키웠는지 추천에 반영합니다. 거의 다 키운 것은 바뀔 아티팩트에 가까운 값어치로 쳐서, 빼거나 다른 것과 바꾸라고 권하지 않습니다.
- 혼자 할 때와 남의 방에 참가했을 때가 같게 동작합니다. 진행도를 아직 확인하지 못한 아이템은 키우지 않은 것으로 두지 않고 그대로 둡니다.

### Changed

- 석판·아티팩트 데이터를 한 번 다시 모읍니다. 게임을 켜면 저절로 이루어지며 그동안 점수가 늦게 뜰 수 있습니다.
- F10 메모 창의 `ESC`가 취소가 됐습니다. 이제 `Enter`는 보내기, `ESC`는 보내지 않기입니다. 취소해도 진단 자료는 이 PC에 그대로 남습니다.
- 전송에 아직 동의하지 않았다면 동의를 먼저 묻고 그다음에 메모를 받습니다.

### Fixed

- 자동 배치로 석판을 돌린 뒤 옛 각도의 범위 테두리가 화면에 남던 문제를 수정했습니다. 가방을 다시 열지 않아도 사라집니다. 방에 참가한 쪽에서는 원래 일어나지 않던 문제입니다.

## 0.3.4

### Added

- F10 진단에 무엇이 이상했는지 적어 함께 보낼 수 있습니다. 증상을 고르거나 직접 적을 수 있고, ESC를 누르면 지금까지처럼 메모 없이 보냅니다.
- 적은 메모는 가리지 않고 그대로 전송합니다. 아이디나 비밀번호처럼 알려지면 안 되는 것은 적지 마세요.
- 진단 전송을 켜지 않았다면 메모를 묻지 않고 지금까지처럼 로컬에만 저장합니다. 이미 전송에 동의했다면 다시 묻지 않습니다.

## 0.3.3

### Changed

- 0.3.0~0.3.2의 DPS 기준 추천과 전투 조건 설정은 포함하지 않습니다. 추천은 0.2.8의 강화 우선·양보와 콤보 기준으로 돌아갔습니다. 그 버전에서 지정한 전용 설정은 파일에 남지만 계산에 쓰이지 않습니다.

### Fixed

- 버릴 수 없는 아티팩트를 제외하거나 새 아이템과 교체하라고 추천하던 문제를 수정했습니다.
- 마음의 짐처럼 자체 활성 효과가 없는 아이템이 유용한 활성 칸을 차지하던 문제를 수정했습니다. 주변 효과와 사용 유지·고정은 계속 고려합니다.
- 대립의 천칭이 자동 배치 중 반대쪽으로 넘어가지 않도록 현재 놓인 쪽을 유지합니다. 방향을 바꾸려면 직접 원하는 쪽으로 옮기세요.
- 추천 화면이 이미 펼쳐진 상태에서 합작 창을 열거나 닫아도 합성 추천 목록이 갱신되도록 수정했습니다.
- 합성 추천에서 손해가 나는 값에 양수 부호가 붙던 표시를 수정했습니다.
- 게임에서 저장한 F10 진단 자료가 정상 입력인데도 재생되지 않던 문제를 수정했습니다.

## 0.3.2

### Added

- F2에서 영원의 식을 자동·왼쪽(얼음 무구 → 화염)·오른쪽(화염검 → 얼음)으로 지정할 수 있습니다. 천칭 방향과 별도로 저장하며 획득·제외·합성 추천에도 적용합니다.

### Changed

- 접힌 추천 화면에 가방 배치를 펼치는 단축키를 표시합니다. DPS 계산의 가정·미반영 안내와 자동 배치 제한을 구분했습니다.
- 자동 배치 제한 사유에 아이템별 위치·유효 레벨·활성 변경을 표시합니다. 헬레나의 계단 모형은 시간제한 효과를 예측하지 못하는 범위를 안내하고 관련 변경의 보호를 유지합니다.

### Fixed

- 열망의 부적의 직접 공격 치명타 보너스와 베루트의 낫의 처형 효과가 배치 비교에서 빠지던 문제를 수정했습니다.
- 유리 망치 등의 체력 효과 변경량을 표시합니다. 체력 효과가 그대로인 단순 이동은 이 이유로 자동 배치를 제한하지 않습니다.

## 0.3.1

### Fixed

- 게임에서 저장한 F10 재현 자료를 정상 입력인데도 불일치로 거부하던 문제를 수정했습니다. 0.3.0에서 저장한 자료도 다시 비교할 수 있습니다.
- 일부 마나 비용 감소 효과의 반올림이 게임과 달라 마법 사용 횟수와 예상 피해가 어긋나던 문제를 수정했습니다.
- README의 콤보 발동 표시와 후보 순위 설명을 현재 DPS·빌드 우선 설정에 맞게 바로잡았습니다.

## 0.3.0

### Added

- F2에서 대립의 천칭을 자동·불 우선(왼쪽)·얼음 우선(오른쪽)으로 설정하고, 현재·추천 위치의 능력치와 방향 충돌 사유를 확인할 수 있습니다.
- F2에 단일·다수 대상, 비교 시간과 시작 자원, 공격 순서와 마법 우선순위 설정을 추가했습니다.
- 현재·추천 배치의 전체·초반·후반 DPS와 자원을 비우고 시작한 DPS, 공격별 피해와 계산하지 못한 효과를 확인할 수 있습니다.
- 단기·장기 비교 프리셋과 추가 대상 적중 비율, 측정한 평균 공격 간격으로 시간을 보정하는 기능을 추가했습니다. 무기가 바뀌면 이전 무기의 보정은 적용하지 않습니다.
- F10 재현 자료로 당시 설정 전체를 유지한 반복 배치 검증을 할 수 있습니다. 적용 제한과 순환 여부도 구분합니다.

### Changed

- 자동 배치와 획득·제외·합성 추천을 예상 DPS 기준으로 전환했습니다. 활성 보호를 유지하고 콤보·프리셋 강제 우선은 별도로 켤 수 있으며 기존 강화·양보 배수는 적용하지 않습니다.
- 미지원 효과가 있는 아이템이나 관련 배치·지원 연결·콤보가 바뀌는 자동 적용은 기본 제한합니다. 추천과 제한 사유를 확인한 뒤 F2에서 별도로 허용할 수 있습니다.
- F10 계획 재현에 전투 조건과 예상 피해 내역을 함께 저장합니다.

### Fixed

- 합작 NPC 창을 열거나 닫을 때 이미 펼쳐진 추천 화면이 이전 상태로 남을 수 있던 문제를 수정했습니다. 상인과 대화하지 않아도 합작 추천을 확인할 수 있습니다.
- 마음의 짐처럼 자체 활성 효과가 없는 아티팩트가 유용한 아이템의 활성 칸을 차지하던 문제를 수정했습니다. 주변 효과와 사용 유지·고정을 고려해 음수 칸을 활용합니다.
- 대립의 천칭의 좌우 능력치 변화가 예상 DPS에서 빠지던 문제를 수정했습니다. 영원의 식에 따른 속성 전환과 함께 평가합니다.
- 버릴 수 없는 아티팩트를 빼거나 교체하라고 추천하던 문제를 수정했습니다.
- 대시 피해 옵션이 선택한 대시 공격의 예상 피해에 반영되도록 수정했습니다.
- 영원의 식을 옮겼을 때 지원하는 얼음 무구와 화염검의 참조 속성이 바뀌는 효과를 평가합니다.
- 마나 회복을 기다리는 마법 계산이 멈추거나, 짧은 공격 주기에서 종료 시점의 공격을 추가로 세고 적중 후 발동을 빠뜨리던 문제를 수정했습니다.
- F2를 열어 둔 상태에서 새 DPS 결과가 표시되지 않던 문제를 수정했습니다. 계산 대기·실패 시 이전 결과임을 표시합니다.
- 소지금만 바뀌어도 배치 계산을 불필요하게 다시 시작하던 문제를 수정했습니다.
- 콤보 단계가 낮아진다는 이유로 DPS가 높아지는 제외 추천을 누락하던 문제를 수정했습니다. 합성·제외 추천에서도 예상 피해와 계산 누락을 확인할 수 있습니다.
- 일부 무기 속성·능력치 식의 기본 피해를 0으로 처리하던 문제와 합성 추천의 음수 표시를 수정했습니다.
- 능력치에 따라 속성이 바뀌는 무기와 혼돈·복합 속성의 적 저항을 반영했습니다. 저항 계산 순서 차이로 피해가 낮아지던 경계 오류도 수정했습니다.

## 0.2.8

### Added

- F10 진단을 비공개 서버로 보내고 제보 번호를 확인하는 기능을 추가했습니다. 처음 동의한 뒤 자동 전송하며 F3에서 끌 수 있습니다.

### Fixed

- F2에서 선택한 콤보를 북향의 금빛 침·푸른 침 배치가 따르지 않던 문제를 수정했습니다. 지정한 콤보를 가진 공격 가능한 대상을 연결하며, 만족할 수 없으면 이유를 표시합니다.
- 합작 NPC의 석판 합성 창을 열어도 추천 화면이 접힌 채 남던 문제를 수정했습니다. 상인에게 말을 걸지 않아도 합성 추천을 확인할 수 있습니다.
- F10을 연속으로 눌러 진단이 반복 전송되지 않도록 전송 간격을 1분으로 제한했습니다. 그 사이에 수집한 진단은 로컬에 저장합니다.
- 진단 전송 동의 창에서 본문이 창 밖으로 넘치고 버튼과 겹치던 문제를 수정했습니다.
- 진단 저장 일부가 실패했을 때 이전 파일이 이번 제보에 섞이지 않도록 수집 기록을 따로 보관합니다.

## 0.2.7

### Added

- F2 빌드 창에 `끄기 허용`을 추가했습니다. 강화 칸을 양보하는 것과 효과를 꺼도 되는 것을 따로 지정할 수 있습니다.

### Changed

- 기본 배치에서 아티팩트의 활성 상태와 지원 연결을 우선합니다. 같은 효과에서는 감점이 없는 빈칸을 더 확보하고, 이동은 그다음에 줄입니다.
- 주력 마법에 적용되는 것으로 확인된 마법 치명타와 마나 회복 효과에 강화 우선 선호를 반영합니다. 능력치 패널티는 그대로 평가합니다.

### Fixed

- 정상 빈칸이 있는데도 보석 갑옷·은팔찌처럼 이득과 패널티가 함께 있는 아티팩트를 음수 칸으로 보내 효과를 끄던 문제를 수정했습니다. 활성 보호는 모든 아티팩트에 공통으로 적용합니다.
- 경쟁 같은 석판을 옮기거나 돌려 같은 강화를 유지하면서 감점을 밖으로 뺄 수 있어도 불필요한 감점 칸을 남기던 배치 선택을 개선했습니다.
- 활성 복구나 감점 칸 정리로 배치가 달라져도 점수만 보고 변경이 없다고 표시하던 문제를 수정했습니다.
- 아이템 획득·제거·합성 추천에서도 현재 사용 중인 다른 아티팩트의 활성 상태를 보호하도록 수정했습니다.
- 이미 합성한 석판이 가방에 있을 때 다른 재료의 합성 추천에서 엉뚱한 석판을 뺀 상태로 평가하던 문제를 수정했습니다.
- `사용 유지`와 `끄기 허용`을 함께 지정했을 때 끄기 허용의 저장 상태가 꺼진 것처럼 보이던 문제를 수정했습니다. 사용 유지가 우선하는 동작은 유지됩니다.
- 석판이 많을 때 배치 계산에 오래 걸리던 문제를 개선했습니다.

## 0.2.6

### Added

- 레이에 별조각이 왼쪽 마법의 마나 비용을 줄이는 효과를 배치에 반영합니다. `사용 유지`는 마법 연결도 지키며, 모래시계와 별조각이 같은 마법을 지원하면 함께 옮기는 배치도 검토합니다.
- 다용도 벨트의 빠른 슬롯 수량, 검은 비늘의 석판 수량, 캘세더니 열쇠의 행별 추가 능력치를 자동 배치 점수에 반영합니다.
- F10 진단에 당시 인벤토리와 빌드 설정을 담은 계획 재현 자료를 추가했습니다. 데이터 갱신 뒤에도 저장한 계획을 다시 비교할 수 있습니다.

### Changed

- F10 진단에 능력치 전환의 적용 상태와 마지막으로 읽은 능력치·타이머, 하얀 종이의 콤보 갱신 순서를 추가해 배치 전후 차이를 더 자세히 확인할 수 있습니다.
- 침과 마법 지원 효과가 연결된 배치를 검토할 때 불필요한 메모리 사용을 줄였습니다.

### Fixed

- 하얀 종이가 양옆의 북향의 침이 물려받은 콤보를 인식하지 못하던 문제를 수정했습니다. 침이 다른 침을 거쳐 같은 콤보를 물려받는 경우도 반영합니다.
- 종이와 침의 콤보에 원래 카테고리가 중복으로 더해질 수 있던 문제를 수정했습니다. 종이끼리 연결된 경우에는 최근 관측값으로 추정하며, 이동 후 결과가 달라질 수 있음을 표시합니다.
- 침의 `사용 유지`가 연결 끝의 공격 대상을 보호하도록 수정했습니다. 연결된 침 사슬을 함께 옮기며, 비활성 침에 남는 기본 피해 보너스도 반영합니다.
- 북향의 침이 공격하지 않는 마법을 강화 대상으로 인정할 수 있던 문제를 수정했습니다. 실제 공격 가능 여부를 콤보 상속과 대상 판정에 반영합니다.
- 후보 추천을 껐을 때 F2 콤보 변경이 배치에 반영되지 않거나, 변경 전 계획이 자동 적용될 수 있던 문제를 수정했습니다.
- 일부 특수 콤보의 발동 단계가 빠져 완성 여부와 추천을 잘못 판단할 수 있던 문제를 수정했습니다. 효과 설명이 비어 있어도 발동 단계는 반영합니다.
- 콤보 데이터를 읽지 못했을 때 일부 단계만 저장하던 처리를 수정했습니다. 이제 데이터 갱신 실패를 알리고 불완전한 결과를 저장하지 않습니다.

## 0.2.5

### Added

- F2 빌드 창에 `빌드 초기화` 버튼을 추가했습니다. 가져온 빌드 코드와 콤보·강화 우선·양보·사용 유지·고정을 한 번에 해제하며, F3의 단축키와 화면 설정은 유지합니다.

### Fixed

- 빛나는 모래시계가 오른쪽 마법과 떨어져도 같은 가치로 평가되던 문제를 수정했습니다. 이제 강화 효과를 반영하고 모래시계와 마법을 함께 옮기는 배치도 검토합니다.
- 모래시계의 `사용 유지`는 사용 가능한 오른쪽 마법과의 연결도 지킵니다. 연결할 수 없으면 이유를 알리고 자동 배치를 멈추며, 연결을 끊는 빼기·교체·합성 추천도 제외합니다.

## 0.2.4

### Added

- 빌드 창에 `사용 유지`를 추가했습니다. 지정한 아티팩트의 활성 상태를 우선하고 빼기·교체 추천에서 보호하며, 유지할 배치를 찾지 못하면 이유를 알리고 자동 배치를 멈춥니다.
- 설정 창의 단축키 탭에서 일반 키와 Ctrl·Alt·Shift 조합을 직접 지정하고, 해제하거나 기본값으로 복원할 수 있습니다. 게임 기본 키와 이 도구의 다른 단축키가 겹치면 안내합니다.

### Changed

- 단축키가 겹쳐도 다른 기능의 키를 자동으로 바꾸지 않습니다. 키를 지정하는 동안에는 이 도구의 단축키 실행을 멈춥니다.

### Fixed

- 강화 우선 가중치가 아티팩트의 패널티까지 키우던 문제를 수정했습니다. 조화의 수정 등 이웃에 따라 달라지는 효과에도 이득 가중치를 반영합니다.
- 자리 교환을 검토할 때 바뀐 배치의 활성 조건과 석판 효과를 다시 확인해, 이전 배치의 조건으로 교환을 평가하던 문제를 수정했습니다.

## 0.2.3

### Added

- 아티팩트나 석판 하나를 제외하고 재배치하면 점수가 오르는 후보를 안내합니다. 콤보 단계가 유지되는 후보만 표시하며 실제 제거는 자동으로 실행하지 않습니다.

### Changed

- 일부 효과만 측정된 아티팩트는 툴팁과 후보 안내에 추정치가 포함됐음을 표시합니다.

### Fixed

- 자물쇠의 양옆 빈칸을 함께 확보해야 하는 배치를 추가로 탐색해, 켤 수 있는 자물쇠를 놓치던 경우를 줄였습니다.
- 플라즈마 헬멧처럼 전환·특수 효과를 환산하지 못한 아티팩트를 손해만 보고 꺼두도록 권하던 평가를 수정했습니다. 봉인된 테자스, 플리트비체의 물방울, 서고의 태양에도 같은 평가 보완을 적용합니다.

## 0.2.2

### Added

- F2에서 선택한 콤보를 캘세더니 열쇠와 하얀 종이의 배치에도 우선 반영합니다. 여러 콤보를 선택하면 진행과 배치 가치를 비교하고, 배치를 찾지 못하면 이유를 안내합니다.

### Fixed

- 친타마니가 발동한 칸에 남는 +3 보너스를 반영해 자동 배치가 잠기던 문제를 고쳤습니다. 싱글과 멀티플레이 모두 적용됩니다.
- 하얀 종이가 양옆의 캘세더니 열쇠에서 현재 행의 콤보를 물려받는 배치를 제대로 계산합니다.

## 0.2.1

`F8` 을 눌러도 계획이 끝나지 않던 문제를 고친 판입니다. 제안이 옮기는 수고를 셈에 넣고, 강화
우선을 내리는 쪽과 제한 해제 칸 고정을 지정할 수 있습니다.

### Added

- 제한 해제 칸 고정. `F2` 빌드 창에서 아티팩트 줄 오른쪽의 `고정`을 누르면 그 아티팩트를 배치
  조건을 무시하는 칸에만 앉힙니다. 자물쇠처럼 조건이 까다로운 아티팩트가 돌릴 때마다 자리를
  옮기던 것을 막습니다. 그런 칸이 모자라면 경고가 뜨고 그 고정만 무시합니다.
- 강화 우선을 내리는 쪽으로도 지정할 수 있습니다. `F2` 빌드 창에서 아티팩트를 우클릭할 때마다
  `▽`(0.5배) → `▽▽`(0.25배) → `▽▽▽`(0.1배) → 해제 로 돌아가고, 지정한 아티팩트는 좋은 칸을
  남에게 양보합니다. 가방이 좁을 때 쓰세요.

### Changed

- 가져온 빌드가 즐겨찾기로 찍은 아티팩트는 후보로 뜨면 맨 위로 올라갑니다. 전에는 가산점만 있어
  배치 이득이 큰 다른 후보에 밀릴 수 있었습니다. `F2` 창의 아티팩트 목록에도 그 빌드의 아티팩트가
  가방에 있는지 없는지가 함께 나옵니다.
- 제안이 옮기는 수고를 셈에 넣습니다. 자리 하나를 옮기는 데 0.3점을 비용으로 쳐서, 이득이 그보다
  작은 이동은 제안하지 않습니다. 아이템 하나를 주웠다고 판 전체를 뒤집으라고 하던 것이 이것으로
  줄어듭니다. 화면의 점수 자체는 예전과 같은 단위입니다.

### Fixed

- 후보 미리보기의 점수를 격자와 일치시켰습니다. 미리보기 중 자동 배치 키를 누르면 현재 가방 배치로 돌아오며 다시 눌러야 적용합니다.
- 프리셋 교체·삭제 시 가져온 콤보 우선순위가 남지 않도록 고쳤습니다. 수동 지정은 유지하고 회피 콤보의 추천 미반영을 표시합니다.
- 빌드 설정 저장 실패를 화면에 표시하고 손상된 설정은 덮어쓰기 전에 백업합니다.
- 가방이 꽉 찼을 때도 칸 교환으로 수동 배치를 안내합니다. 추천 목록만 다시 계산하는 동안에는 검증된 동일 가방 배치를 적용할 수 있습니다.
- 후보 추천을 끄면 주변 후보를 읽지 않으며 카탈로그 재생성이나 종료 시 남은 배치 계산을 취소합니다.
- 참가자로 접속한 멀티플레이에서 신비 콤보의 고정 각인이 계산에서 빠져 자동 배치가 잠기던 문제를 고쳤습니다.
- 자동 배치는 마지막 위치와 회전이 목표와 같은지 확인합니다. 도중에 바뀐 아이템은 추가로 옮기거나 되돌리지 않습니다.
- 서버 반영이 확인되지 않은 이동·회전은 늦게 적용될 가능성을 알리고 추가 자동 배치를 잠급니다. 이때는 방에 재접속한 뒤 다시 사용할 수 있습니다.
- 인게임 화면을 꺼도 빌드 창의 우클릭으로 강화 우선을 내릴 수 있습니다. 자동 배치가 잠긴 화면에서 자동 배치를 이용하라는 중복 안내도 없앴습니다.
- 레벨이 0인 칸에서도 배수와 제한 해제 효과를 대조합니다. 불일치 안내는 칸 수가 아닌 검증 항목 수로 표시합니다.
- 설치 안내가 멀티플레이 자동 배치를 방을 연 쪽에서만 된다고 적고 있던 문제. 참가한 쪽에서도
  됩니다.
- 참가한 쪽의 자동 배치가 30초 상한에 걸릴 때, 마지막으로 보낸 걸음이 뒤늦게 반영되면 되돌리기가
  그 걸음을 놓칠 수 있던 문제. 이제 상한이 가까우면 걸음을 보내지 않습니다.
- 접힌 화면에서 가방이 꽉 찼을 때 같은 안내가 두 번, 그중 하나는 잘린 채로 뜨던 문제.
- 북향의 침이나 캘세더니 열쇠가 있는 가방에서 `F8` 을 눌러도 계획이 끝나지 않고 매번 몇 수씩
  또 나오던 문제. 침과 열쇠는 자리에 따라 콤보 개수를 바꾸는데, 그 몫이 이미 들어간 개수로
  다음 자리를 재는 바람에 두 콤보 사이를 오갔습니다. 이제 한 번 적용하면 멈춥니다.

## 0.2.0

자동 배치가 고르는 배치가 나아졌습니다. 자리에 따라 효과가 달라지는 아티팩트 넷도 새로
계산에 들어갑니다.

처음 켤 때 데이터를 다시 만듭니다. 잠깐 걸리고 그 뒤로는 예전과 같습니다.

### Added

- 북향의 금빛 침, 북향의 파란 침. 바로 위 칸의 아티팩트를 강화하는 효과를 계산합니다.
  대상이 없는 자리에는 세우지 않고, 침 위에 침을 쌓는 배치도 함께 봅니다.
- 거대한 망원경. 이웃 여덟 칸의 행성을 강화하는 효과를 계산합니다.
- 헌신의 휘장. 같은 줄의 동료에게 걸리는 효과를 계산합니다.
- 캘세더니 열쇠. 놓인 줄에 따라 콤보 종류가 바뀌는 것을 계산합니다.
- 강화 우선 지정에 세기가 생겼습니다. `F2` 빌드 창에서 아티팩트를 누를 때마다
  `★`(2배) → `★★`(4배) → `★★★`(10배) → 해제 로 돌아갑니다.
- 멀티플레이 자동 배치를 다시 열 수 있습니다. `F3` 설정 창의 "멀티 자동 배치(실험)"이고
  기본은 꺼져 있습니다. 방을 연 쪽과 참가한 쪽 모두 동작합니다. 켜기 전에 같이 하는 분들의
  동의를 받으세요.
- `F10` 이 재생용 스냅샷(`inventory-snapshot.json`)도 함께 남깁니다.
- 설정 창(`F3`)에서 화면 갱신 주기와 후보 추천 범위를 바꿀 수 있습니다. 전에는 설정 파일을
  직접 고쳐야 닿을 수 있었습니다. 게임이 무거우면 갱신 주기를 늘려 보세요.

### Changed

- 설치가 한 번에 끝납니다. BepInEx를 따로 받지 않아도 되도록 함께 담았습니다. zip 안의
  "게임 폴더에 복사" 폴더에 있는 것을 게임 폴더에 넣으면 됩니다. 이미 BepInEx를 쓰고 계셨다면
  `BepInEx\plugins` 의 DLL 두 개만 덮어쓰세요.
- 자동 배치가 후보를 고르는 방식을 바꿨습니다. 같은 가방에서 더 나은 배치를 내놓습니다.
  계산이 조금 느려졌고, 같은 석판을 여러 장 들고 있는 가방은 오히려 빨라집니다.
- 참가한 쪽에서 자동 배치를 돌리면 몇 초 걸립니다. 한 걸음씩 서버에 보내고 반영을 확인한 뒤
  다음으로 가기 때문입니다. 도는 동안 몇 번째 걸음인지가 화면에 뜨고, 30초가 넘으면 멈춥니다.
  방을 연 쪽에서는 예전처럼 즉시 끝납니다.
- 데이터 갱신이 실패해도 이전 데이터로 계속 돕니다. 전에는 그 세션을 못 썼습니다.
- 자동 배치 결과가 화면에 더 오래 남습니다. 실패했을 때의 안내가 길어 다 읽기 전에 사라지곤
  했습니다.
- `F8` 이 계산 중이라 거절될 때 잠시 뒤 다시 누르라고 알려줍니다.

### Fixed

- 강화 우선으로 찍은 아티팩트가 석판 배치에 제대로 반영되지 않던 문제. 0.1.3 설명과 달리
  일부 경우에만 동작했습니다.
- 제안이 가만히 있어도 계속 바뀌던 문제. 가방이 거의 찼을 때 특히 심했습니다.
- 같은 석판을 여러 장 들고 있으면 같은 배치를 여러 번 계산하던 문제.
- 화면 아래 안내문이 잘려 나오던 문제.
- 자동 배치가 도는 동안에도 계산이 계속 돌아 프레임을 갉아먹던 문제.
- 자동 배치가 멈춘 채로 남아 `F8` 이 세션 내내 잠기던 문제.
- 놓을 자리가 없는 석판이 있을 때 `F8` 이 "옮길 것이 없습니다"라고 하던 문제.
- 멀티 자동 배치를 켜고 끈 직후 화면 안내가 늦게 바뀌던 문제.
- 참가자 세션에서 석판을 돌릴 때 엉뚱한 칸을 누를 수 있던 문제.
- 참가자 세션에서 되돌리기가 실패해도 되돌렸다고 알리던 문제.
- `F10` 덤프가 다른 플레이어의 인벤토리 칸 수까지 적던 문제.
- 설정 파일에 이상한 값이 들어가면 인벤토리를 매 프레임 읽을 수 있던 문제.
- 빌드 창의 강화 우선 설명이 세기와 상관없이 "2배"라고만 적혀 있던 문제.
- 후보 미리보기에서 그 후보와 상관없는 칸까지 금색으로 짚을 수 있던 문제.
- 타이틀과 로비 화면에서 하지 않아도 되는 검사를 계속 돌던 문제.
- 빌드 설정을 저장하지 못해도 아무 말이 없던 문제. 이제 로그에 남습니다.
- 빌드 창에서 강화 우선을 누르면 가방에 있는 아티팩트가 목록에서 사라지고 지정해 둔 것만
  "가방에 없음"으로 뜨던 문제. 누른 순간에 따라 그랬다 말았다 했습니다.

## 0.1.3

제안이 저절로 바뀌던 것과, 데이터 갱신이 한 번 실패하면 그 세션을 못 쓰게 되던 것을 고친 판입니다.
강화 우선 지정에 세기가 생겼습니다.

### 새로 생긴 것

- **강화 우선 지정에 세기가 생겼습니다.** `F2` 빌드 창에서 아티팩트를 누를 때마다
  `★`(2배) → `★★`(4배) → `★★★`(10배) → 해제 로 돌아갑니다. 전에는 켜고 끄기뿐이라 2배로
  고정이었습니다. 빌드의 축이 되는 아티팩트를 무겁게 찍어 두면 **석판 배치까지 그 칸의 레벨을
  올려 주는 쪽으로 따라옵니다.** 몇 단계인지는 격자 칸에 커서를 올리면 나옵니다.
  - 세기는 "값어치 차이를 어디까지 밀어낼 수 있는가"로 정했습니다. 2배는 비슷한 것들 사이에서
    앞서고, 4배는 웬만한 것보다 앞서고, 10배는 다른 것을 희생해서라도 최우선입니다.
  - 레벨 상한 위로는 올리지 못합니다. 상한에 닿은 뒤로는 그 아티팩트가 꺼지거나 밀려나지 않게
    버티는 데만 쓰입니다.
  - 전에 지정해 둔 것은 `★` 한 단계로 옮겨집니다.

- **멀티플레이 자동 배치를 다시 열 수 있습니다(실험, 기본 꺼짐).** `F3` 설정 창의
  "멀티 자동 배치(실험)"입니다. 개발사가 금지한 것이 아니라 인벤토리 동기화 방식을 손보는 중이라
  잠가 두는 편이 안전하다고 한 것이어서, 기본은 꺼 두고 켜는 길만 남겼습니다. 공식 FAQ 가 멀티
  모드를 "참가자 전원 동의" 조건으로 두므로 켜기 전에 같이 하는 분들의 동의를 받으세요.
  - **방을 연 쪽이든 참가한 쪽이든 동작합니다.** 한동안 "참가자 쪽에서는 게임 구조상 안 된다"고
    적어 두었는데 그것이 틀렸습니다 — 석판을 돌리는 길을 하나 놓치고 있었습니다.
  - 참가한 쪽에서는 **옮길 것이 많으면 몇 초 걸립니다.** 한 걸음씩 서버에 보내고 반영된 것을
    확인한 뒤 다음 걸음으로 가기 때문입니다. 확인이 안 되면 거기서 멈추고 이미 옮긴 것을
    되돌립니다. 방을 연 쪽에서는 예전처럼 즉시 끝납니다.
  - 각인이 걸린 판에 참가자로 들어가 있으면 자동 배치가 잠깁니다. 각인은 방을 연 쪽에서만 읽을
    수 있어서, 그대로 두면 어긋난 계산으로 배치하게 되기 때문입니다.

### 고침

- **제안이 가만히 있어도 계속 바뀌던 것을 고쳤습니다.** 가방이 거의 찼을 때 특히 심했습니다 —
  제안대로 하나씩 옮기는 동안 석판 추천이 계속 다시 쓰였습니다. 원인이 둘이었습니다.
  - 효과가 꺼진 아티팩트는 어느 칸에서도 0점이라, 배치를 풀 때마다 아무 데나 보내고 있었습니다.
    그 자리가 흔들리면 이웃을 보는 아티팩트들이 함께 흔들려 석판 배치의 점수까지 달라졌습니다.
  - 배치 조건이 서로 물리면 계산이 두 상태를 오갈 수 있는데, 그중 나쁜 쪽에서 멈춘 답을 그대로
    쓰고 있었습니다. 이제 지나온 것 중 가장 좋은 것을 씁니다.
- **`F9` 가 실패하면 그 세션 내내 아무것도 되지 않던 문제를 고쳤습니다.** 누르는 순간 이전
  데이터를 버려서, 만드는 데 실패하면 되살릴 길도 다시 시도할 길도 없었습니다. 화면에는
  "데이터 준비 중"만 계속 떴습니다. 이제 새로 만드는 데 실패해도 **이전 데이터로 계속 돌아가고**,
  실패했다는 것과 다시 시도하는 방법이 화면에 뜹니다. 다음 탐험을 시작할 때 한 번 더 시도합니다.
  덤프가 성공하면 예전 세대 폴더도 정리합니다(전에는 누를 때마다 한 벌씩 쌓였습니다).
- **접었을 때 안내 줄이 화면 밖으로 흘러넘치던 것을 고쳤습니다.** 접힌 폭이 아니라 펼친 폭으로
  높이를 재고 있었습니다.
- **경고가 첫 줄만 보이던 것을 고쳤습니다.** 경고가 둘 이상이면 "멀티플레이 세션 - 제안만
  표시합니다" 같은 뒤쪽 안내가 아예 보이지 않았습니다.
- **화면을 그리다 오류가 나면 반쯤 그려진 채로 굳던 것을 고쳤습니다.**
- **창을 연 채로 씬이 바뀌면 시간이 멈춘 채 남을 수 있던 것을 고쳤습니다.**
- **자동 배치를 되돌릴 때 실제로 되돌아갔는지 확인합니다.** 게임이 거부해도 예외가 나지 않아,
  되돌리지 못했는데 "원래 배치로 되돌렸습니다"라고 할 수 있었습니다.
- **설정 값에 범위가 생겼습니다.** 특히 인벤토리를 다시 읽는 주기가 설정 파일에 이상한 값으로
  들어가면 매 프레임 읽을 수 있었습니다.

### 그 밖에

- **`F10` 이 재생용 스냅샷(`inventory-snapshot.json`)도 함께 남깁니다.** 이상한 점을 제보할 때
  이 파일을 함께 주시면 그 상황을 그대로 재현해 볼 수 있습니다.
- **어느 버전이 어느 게임 위에서 돌고 있는지가 로그와 `F10` 덤프 첫 줄에 적힙니다.**
- **`F10` 덤프에서 화면에 보이지 않는 것을 뺐습니다.** 열지 않은 상자의 개수나 아직 열지 않은
  세피라이트의 보상 번호가 적히고 있었습니다. 이 도구가 화면에서 볼 수 없는 것을 알려주지
  않는다는 원칙에 어긋나는 자리였습니다.

## 0.1.2

게임 루프 안에서 남아 있던 끊김과 시작 화면의 진입 지연을 고친 판입니다. 보여 주는 내용과 조작은
0.1.1과 같습니다.

### 고침

- **플레이 중 남아 있던 끊김을 없앴습니다.** 0.1.1 이 계산 쪽을 고쳤다면 이번에는 게임 루프
  안에서 도는 몫입니다. 플러그인이 초당 네 번 씬 전체를 세 번씩 훑고 있었는데, 실기에서 재어 보니
  **한 번의 폴링이 15.2ms** 였습니다. 60fps 기준 한 프레임을 통째로 넘기는 값을 초당 네 번 쓰고
  있었던 셈입니다. 지금은 **1.9ms** 이고, 메인 스레드 점유는 6.1% 에서 0.77% 가 됐습니다.
  - 세피라이트는 **보상 창이 열려 있을 때만** 찾습니다. 창이 닫혀 있으면 후보가 나올 수 없는데도
    매번 찾고 있었습니다(7.19ms → 0.15ms).
  - 석판 합성기와 상자·상점은 찾은 결과를 잠시 들고 씁니다. 뚜껑이 열리는 것은 이미 있던 것의
    상태 변화라 **여는 순간이 늦어지지 않습니다**(합성기 4.37ms → 0.24ms, 상자 3.96ms → 1.17ms).
- **시작 화면에서 인게임에 들어갈 때 오래 걸리던 문제를 고쳤습니다.** 게임이 막 켜진 동안에는
  아직 글자 표가 준비되지 않아 플러그인이 데이터를 짓는 데 실패하는데, 그때마다 게임의 아이템
  리소스를 통째로(다섯 번씩) 다시 훑고 있었습니다. 준비될 때까지 초당 스무 번꼴이라, 느린 PC 일수록
  그 구간이 길어졌습니다. 이제 한 번만 훑고 들고 있다가 다 지으면 놓아줍니다.

### 그 밖에

- **F10 진단 덤프에 `[perf]` 절이 생겼습니다.** 플러그인이 게임 루프 안에서 실제로 쓴 시간을
  구간별로 평균과 최악까지 적습니다. 프레임 몇 번 중 화면을 다시 그렸는지, 시작할 때 데이터를
  몇 번 만에 지었는지도 함께 적힙니다. 끊긴다는 제보를 받았을 때 추측 대신 이 숫자로 답하기 위한
  것이며, 같은 요약이 5분마다 BepInEx 로그에도 한 줄 남습니다.

## 0.1.1

프레임 하락을 고친 판입니다. 보여 주는 내용과 조작은 0.1.0과 같습니다.

### 고침

- **플레이 중 프레임이 크게 떨어지던 문제를 고쳤습니다.** 계산이 백그라운드 스레드에서 도는데도
  게임이 끊긴 것은, 그 계산이 만들어 내는 쓰레기를 치우느라 GC 가 게임 전체를 세웠기 때문입니다.
  가방이 꽉 찬 채로 상자를 열면 한 번의 계산이 26초에 40GB 를 썼습니다. 같은 상황이 지금은
  0.9초에 1.4GB 입니다.
  - 후보 추천이 "무엇을 밀어낼까"를 따질 때마다 석판 배치를 처음부터 다시 찾던 것을, 배치는
    한 번만 찾고 아티팩트 배정만 다시 풀도록 바꿨습니다. 추천 내용은 이전과 같습니다.
  - 합성 추천이 석판 쌍마다 판을 다시 풀던 것을, 먼저 짐작으로 줄을 세우고 보여줄 것만 제대로
    풀도록 바꿨습니다.
  - **동전을 주울 때마다 계산이 다시 돌던 것을 멈췄습니다.** 소지금은 "지금 살 수 있는가"에만
    쓰이므로, 그 답이 뒤집힐 때만 다시 계산합니다.
  - 화면이 매 프레임 격자와 목록을 통째로 다시 만들던 것을, 내용이 바뀔 때만 그리도록 했습니다.
    설명 쪽지도 커서가 올라간 것 하나만 만듭니다.
  - 상자도 상점도 열지 않았을 때는 후보 계산을 아예 건너뜁니다.
  - 계산이 끝나기 전에 상황이 바뀌면 그 계산을 멈춥니다. 어차피 버릴 답이었습니다.

## 0.1.0

첫 공개 버전입니다.

- 인게임 HUD에 현재 배치의 점수, 더 나은 배치, 옮길 순서를 보여 줍니다. 게임 입력은 가져가지
  않고 조작은 F1~F10 단축키입니다.
- 상자·상점·세피라이트를 열면 무엇을 집을지 후보를 줄 세웁니다. 순위는 참고용이며, 가방이
  찼을 때 무엇이 빠지는지와 콤보 진행도 함께 보여 줍니다.
- 싱글플레이에서 F8 한 번으로 계산된 배치를 적용합니다. 적용 직전 게임 상태를 다시 검증하고,
  도중에 실패하면 옮긴 것을 되돌립니다. 멀티플레이에서는 보여 주기만 하며 켜는 옵션이 없습니다.
- F2 빌드 창에서 프리셋 코드 붙여넣기, 밀고 있는 콤보 지정, 강화 우선 아티팩트 지정을 합니다.
- F3 설정 창에서 위치·크기·폭·불투명도·후보 추천·단축키를 바꿉니다.
- F1로 후보를 집었을 때의 격자를 미리 봅니다.
- F9로 석판·아티팩트 데이터를 게임에서 읽어 옵니다. 게임이 패치되면 한 번 더 누르면 됩니다.
