# 변경 기록

## 0.2.0

자동 배치가 고르는 배치가 나아졌습니다. 자리에 따라 효과가 달라지는 아티팩트 넷도 새로
계산에 들어갑니다.

0.1.3 은 따로 배포하지 않았으므로 그 내용도 함께 들어 있습니다.

처음 켤 때 데이터를 다시 만듭니다. 잠깐 걸리고 그 뒤로는 예전과 같습니다.

### Added

- 북향의 금빛 침, 북향의 파란 침. 바로 위 칸의 아티팩트를 강화하는 효과를 계산합니다.
  대상이 없는 자리에는 세우지 않고, 침 위에 침을 쌓는 배치도 함께 봅니다.
- 거대한 망원경. 이웃 여덟 칸의 행성을 강화하는 효과를 계산합니다.
- 헌신의 휘장. 같은 줄의 동료에게 걸리는 효과를 계산합니다.
- 캘세더니 열쇠. 놓인 줄에 따라 콤보 종류가 바뀌는 것을 계산합니다.
- 강화 우선 지정에 세기가 생겼습니다. `F2` 빌드 창에서 아티팩트를 누를 때마다
  `★`(2배) → `★★`(4배) → `★★★`(10배) → 해제 로 돌아갑니다.
- 멀티플레이 자동 배치를 다시 열 수 있습니다. `F3` 설정 창의 "멀티 자동 배치(실험)"이고
  기본은 꺼져 있습니다. 방을 연 쪽과 참가한 쪽 모두 동작합니다. 켜기 전에 같이 하는 분들의
  동의를 받으세요.
- `F10` 이 재생용 스냅샷(`inventory-snapshot.json`)도 함께 남깁니다.
- 설정 창(`F3`)에서 화면 갱신 주기와 후보 추천 범위를 바꿀 수 있습니다. 전에는 설정 파일을
  직접 고쳐야 닿을 수 있었습니다. 게임이 무거우면 갱신 주기를 늘려 보세요.

### Changed

- 자동 배치가 후보를 고르는 방식을 바꿨습니다. 같은 가방에서 더 나은 배치를 내놓습니다.
  계산이 조금 느려졌고, 같은 석판을 여러 장 들고 있는 가방은 오히려 빨라집니다.
- 참가한 쪽에서 자동 배치를 돌리면 몇 초 걸립니다. 한 걸음씩 서버에 보내고 반영을 확인한 뒤
  다음으로 가기 때문입니다. 도는 동안 몇 번째 걸음인지가 화면에 뜨고, 30초가 넘으면 멈춥니다.
  방을 연 쪽에서는 예전처럼 즉시 끝납니다.
- 데이터 갱신이 실패해도 이전 데이터로 계속 돕니다. 전에는 그 세션을 못 썼습니다.
- 자동 배치 결과가 화면에 더 오래 남습니다. 실패했을 때의 안내가 길어 다 읽기 전에 사라지곤
  했습니다.
- `F8` 이 계산 중이라 거절될 때 잠시 뒤 다시 누르라고 알려줍니다.

### Fixed

- 강화 우선으로 찍은 아티팩트가 석판 배치에 제대로 반영되지 않던 문제. 0.1.3 설명과 달리
  일부 경우에만 동작했습니다.
- 제안이 가만히 있어도 계속 바뀌던 문제. 가방이 거의 찼을 때 특히 심했습니다.
- 같은 석판을 여러 장 들고 있으면 같은 배치를 여러 번 계산하던 문제.
- 화면 아래 안내문이 잘려 나오던 문제.
- 자동 배치가 도는 동안에도 계산이 계속 돌아 프레임을 갉아먹던 문제.
- 자동 배치가 멈춘 채로 남아 `F8` 이 세션 내내 잠기던 문제.
- 놓을 자리가 없는 석판이 있을 때 `F8` 이 "옮길 것이 없습니다"라고 하던 문제.
- 멀티 자동 배치를 켜고 끈 직후 화면 안내가 늦게 바뀌던 문제.
- 참가자 세션에서 석판을 돌릴 때 엉뚱한 칸을 누를 수 있던 문제.
- 참가자 세션에서 되돌리기가 실패해도 되돌렸다고 알리던 문제.
- `F10` 덤프가 다른 플레이어의 인벤토리 칸 수까지 적던 문제.
- 설정 파일에 이상한 값이 들어가면 인벤토리를 매 프레임 읽을 수 있던 문제.
- 빌드 창의 강화 우선 설명이 세기와 상관없이 "2배"라고만 적혀 있던 문제.
- 후보 미리보기에서 그 후보와 상관없는 칸까지 금색으로 짚을 수 있던 문제.
- 타이틀과 로비 화면에서 하지 않아도 되는 검사를 계속 돌던 문제.
- 빌드 설정을 저장하지 못해도 아무 말이 없던 문제. 이제 로그에 남습니다.

## 0.1.3

제안이 저절로 바뀌던 것과, 데이터 갱신이 한 번 실패하면 그 세션을 못 쓰게 되던 것을 고친 판입니다.
강화 우선 지정에 세기가 생겼습니다.

### 새로 생긴 것

- **강화 우선 지정에 세기가 생겼습니다.** `F2` 빌드 창에서 아티팩트를 누를 때마다
  `★`(2배) → `★★`(4배) → `★★★`(10배) → 해제 로 돌아갑니다. 전에는 켜고 끄기뿐이라 2배로
  고정이었습니다. 빌드의 축이 되는 아티팩트를 무겁게 찍어 두면 **석판 배치까지 그 칸의 레벨을
  올려 주는 쪽으로 따라옵니다.** 몇 단계인지는 격자 칸에 커서를 올리면 나옵니다.
  - 세기는 "값어치 차이를 어디까지 밀어낼 수 있는가"로 정했습니다. 2배는 비슷한 것들 사이에서
    앞서고, 4배는 웬만한 것보다 앞서고, 10배는 다른 것을 희생해서라도 최우선입니다.
  - 레벨 상한 위로는 올리지 못합니다. 상한에 닿은 뒤로는 그 아티팩트가 꺼지거나 밀려나지 않게
    버티는 데만 쓰입니다.
  - 전에 지정해 둔 것은 `★` 한 단계로 옮겨집니다.

- **멀티플레이 자동 배치를 다시 열 수 있습니다(실험, 기본 꺼짐).** `F3` 설정 창의
  "멀티 자동 배치(실험)"입니다. 개발사가 금지한 것이 아니라 인벤토리 동기화 방식을 손보는 중이라
  잠가 두는 편이 안전하다고 한 것이어서, 기본은 꺼 두고 켜는 길만 남겼습니다. 공식 FAQ 가 멀티
  모드를 "참가자 전원 동의" 조건으로 두므로 켜기 전에 같이 하는 분들의 동의를 받으세요.
  - **방을 연 쪽이든 참가한 쪽이든 동작합니다.** 한동안 "참가자 쪽에서는 게임 구조상 안 된다"고
    적어 두었는데 그것이 틀렸습니다 — 석판을 돌리는 길을 하나 놓치고 있었습니다.
  - 참가한 쪽에서는 **옮길 것이 많으면 몇 초 걸립니다.** 한 걸음씩 서버에 보내고 반영된 것을
    확인한 뒤 다음 걸음으로 가기 때문입니다. 확인이 안 되면 거기서 멈추고 이미 옮긴 것을
    되돌립니다. 방을 연 쪽에서는 예전처럼 즉시 끝납니다.
  - 각인이 걸린 판에 참가자로 들어가 있으면 자동 배치가 잠깁니다. 각인은 방을 연 쪽에서만 읽을
    수 있어서, 그대로 두면 어긋난 계산으로 배치하게 되기 때문입니다.

### 고침

- **제안이 가만히 있어도 계속 바뀌던 것을 고쳤습니다.** 가방이 거의 찼을 때 특히 심했습니다 —
  제안대로 하나씩 옮기는 동안 석판 추천이 계속 다시 쓰였습니다. 원인이 둘이었습니다.
  - 효과가 꺼진 아티팩트는 어느 칸에서도 0점이라, 배치를 풀 때마다 아무 데나 보내고 있었습니다.
    그 자리가 흔들리면 이웃을 보는 아티팩트들이 함께 흔들려 석판 배치의 점수까지 달라졌습니다.
  - 배치 조건이 서로 물리면 계산이 두 상태를 오갈 수 있는데, 그중 나쁜 쪽에서 멈춘 답을 그대로
    쓰고 있었습니다. 이제 지나온 것 중 가장 좋은 것을 씁니다.
- **`F9` 가 실패하면 그 세션 내내 아무것도 되지 않던 문제를 고쳤습니다.** 누르는 순간 이전
  데이터를 버려서, 만드는 데 실패하면 되살릴 길도 다시 시도할 길도 없었습니다. 화면에는
  "데이터 준비 중"만 계속 떴습니다. 이제 새로 만드는 데 실패해도 **이전 데이터로 계속 돌아가고**,
  실패했다는 것과 다시 시도하는 방법이 화면에 뜹니다. 다음 탐험을 시작할 때 한 번 더 시도합니다.
  덤프가 성공하면 예전 세대 폴더도 정리합니다(전에는 누를 때마다 한 벌씩 쌓였습니다).
- **접었을 때 안내 줄이 화면 밖으로 흘러넘치던 것을 고쳤습니다.** 접힌 폭이 아니라 펼친 폭으로
  높이를 재고 있었습니다.
- **경고가 첫 줄만 보이던 것을 고쳤습니다.** 경고가 둘 이상이면 "멀티플레이 세션 - 제안만
  표시합니다" 같은 뒤쪽 안내가 아예 보이지 않았습니다.
- **화면을 그리다 오류가 나면 반쯤 그려진 채로 굳던 것을 고쳤습니다.**
- **창을 연 채로 씬이 바뀌면 시간이 멈춘 채 남을 수 있던 것을 고쳤습니다.**
- **자동 배치를 되돌릴 때 실제로 되돌아갔는지 확인합니다.** 게임이 거부해도 예외가 나지 않아,
  되돌리지 못했는데 "원래 배치로 되돌렸습니다"라고 할 수 있었습니다.
- **설정 값에 범위가 생겼습니다.** 특히 인벤토리를 다시 읽는 주기가 설정 파일에 이상한 값으로
  들어가면 매 프레임 읽을 수 있었습니다.

### 그 밖에

- **`F10` 이 재생용 스냅샷(`inventory-snapshot.json`)도 함께 남깁니다.** 이상한 점을 제보할 때
  이 파일을 함께 주시면 그 상황을 그대로 재현해 볼 수 있습니다.
- **어느 버전이 어느 게임 위에서 돌고 있는지가 로그와 `F10` 덤프 첫 줄에 적힙니다.**
- **`F10` 덤프에서 화면에 보이지 않는 것을 뺐습니다.** 열지 않은 상자의 개수나 아직 열지 않은
  세피라이트의 보상 번호가 적히고 있었습니다. 이 도구가 화면에서 볼 수 없는 것을 알려주지
  않는다는 원칙에 어긋나는 자리였습니다.

## 0.1.2

게임 루프 안에서 남아 있던 끊김과 시작 화면의 진입 지연을 고친 판입니다. 보여 주는 내용과 조작은
0.1.1과 같습니다.

### 고침

- **플레이 중 남아 있던 끊김을 없앴습니다.** 0.1.1 이 계산 쪽을 고쳤다면 이번에는 게임 루프
  안에서 도는 몫입니다. 플러그인이 초당 네 번 씬 전체를 세 번씩 훑고 있었는데, 실기에서 재어 보니
  **한 번의 폴링이 15.2ms** 였습니다. 60fps 기준 한 프레임을 통째로 넘기는 값을 초당 네 번 쓰고
  있었던 셈입니다. 지금은 **1.9ms** 이고, 메인 스레드 점유는 6.1% 에서 0.77% 가 됐습니다.
  - 세피라이트는 **보상 창이 열려 있을 때만** 찾습니다. 창이 닫혀 있으면 후보가 나올 수 없는데도
    매번 찾고 있었습니다(7.19ms → 0.15ms).
  - 석판 합성기와 상자·상점은 찾은 결과를 잠시 들고 씁니다. 뚜껑이 열리는 것은 이미 있던 것의
    상태 변화라 **여는 순간이 늦어지지 않습니다**(합성기 4.37ms → 0.24ms, 상자 3.96ms → 1.17ms).
- **시작 화면에서 인게임에 들어갈 때 오래 걸리던 문제를 고쳤습니다.** 게임이 막 켜진 동안에는
  아직 글자 표가 준비되지 않아 플러그인이 데이터를 짓는 데 실패하는데, 그때마다 게임의 아이템
  리소스를 통째로(다섯 번씩) 다시 훑고 있었습니다. 준비될 때까지 초당 스무 번꼴이라, 느린 PC 일수록
  그 구간이 길어졌습니다. 이제 한 번만 훑고 들고 있다가 다 지으면 놓아줍니다.

### 그 밖에

- **F10 진단 덤프에 `[perf]` 절이 생겼습니다.** 플러그인이 게임 루프 안에서 실제로 쓴 시간을
  구간별로 평균과 최악까지 적습니다. 프레임 몇 번 중 화면을 다시 그렸는지, 시작할 때 데이터를
  몇 번 만에 지었는지도 함께 적힙니다. 끊긴다는 제보를 받았을 때 추측 대신 이 숫자로 답하기 위한
  것이며, 같은 요약이 5분마다 BepInEx 로그에도 한 줄 남습니다.

## 0.1.1

프레임 하락을 고친 판입니다. 보여 주는 내용과 조작은 0.1.0과 같습니다.

### 고침

- **플레이 중 프레임이 크게 떨어지던 문제를 고쳤습니다.** 계산이 백그라운드 스레드에서 도는데도
  게임이 끊긴 것은, 그 계산이 만들어 내는 쓰레기를 치우느라 GC 가 게임 전체를 세웠기 때문입니다.
  가방이 꽉 찬 채로 상자를 열면 한 번의 계산이 26초에 40GB 를 썼습니다. 같은 상황이 지금은
  0.9초에 1.4GB 입니다.
  - 후보 추천이 "무엇을 밀어낼까"를 따질 때마다 석판 배치를 처음부터 다시 찾던 것을, 배치는
    한 번만 찾고 아티팩트 배정만 다시 풀도록 바꿨습니다. 추천 내용은 이전과 같습니다.
  - 합성 추천이 석판 쌍마다 판을 다시 풀던 것을, 먼저 짐작으로 줄을 세우고 보여줄 것만 제대로
    풀도록 바꿨습니다.
  - **동전을 주울 때마다 계산이 다시 돌던 것을 멈췄습니다.** 소지금은 "지금 살 수 있는가"에만
    쓰이므로, 그 답이 뒤집힐 때만 다시 계산합니다.
  - 화면이 매 프레임 격자와 목록을 통째로 다시 만들던 것을, 내용이 바뀔 때만 그리도록 했습니다.
    설명 쪽지도 커서가 올라간 것 하나만 만듭니다.
  - 상자도 상점도 열지 않았을 때는 후보 계산을 아예 건너뜁니다.
  - 계산이 끝나기 전에 상황이 바뀌면 그 계산을 멈춥니다. 어차피 버릴 답이었습니다.

## 0.1.0

첫 공개 버전입니다.

- 인게임 HUD에 현재 배치의 점수, 더 나은 배치, 옮길 순서를 보여 줍니다. 게임 입력은 가져가지
  않고 조작은 F1~F10 단축키입니다.
- 상자·상점·세피라이트를 열면 무엇을 집을지 후보를 줄 세웁니다. 순위는 참고용이며, 가방이
  찼을 때 무엇이 빠지는지와 콤보 진행도 함께 보여 줍니다.
- 싱글플레이에서 F8 한 번으로 계산된 배치를 적용합니다. 적용 직전 게임 상태를 다시 검증하고,
  도중에 실패하면 옮긴 것을 되돌립니다. 멀티플레이에서는 보여 주기만 하며 켜는 옵션이 없습니다.
- F2 빌드 창에서 프리셋 코드 붙여넣기, 밀고 있는 콤보 지정, 강화 우선 아티팩트 지정을 합니다.
- F3 설정 창에서 위치·크기·폭·불투명도·후보 추천·단축키를 바꿉니다.
- F1로 후보를 집었을 때의 격자를 미리 봅니다.
- F9로 석판·아티팩트 데이터를 게임에서 읽어 옵니다. 게임이 패치되면 한 번 더 누르면 됩니다.
