# 효과 누락과 상호작용 점검

전체 효과를 확인한 뒤 평가 모델을 바꾸는 순서가 맞다. 현재 평가는 정적 능력치 환산과 일부
자리 효과를 지원하지만, 특수 동작을 모두 시뮬레이션하지 않는다. 희귀도 추정은 그 누락을
보충하는 값이지 효과 구현의 증거가 아니다.

## 범위와 근거

- [전체 대응 목록](EFFECT-AUDIT-INVENTORY-2026-09-09.md): 저장 카탈로그의 아티팩트 299종,
  동작 클래스 136종, 석판 68종, 콤보 20종을 현재 어셈블리와 전수 대조했다.
- `Assembly-CSharp.dll` 전체를 `ilspycmd`로 디컴파일하고, 배치·카테고리·연결·능력치 전환
  경로와 활성 조건 10종을 상세 대조했다. 클래스 목록에는 상속 관계와 직접 호출 흔적도 남겼다.
- 카탈로그는 게임 1.0.30에서 저장된 형식 9다. 현재 코드 형식 11의 프리팹 전수 재수집은
  수행하지 않았다. 따라서 299종을 현재 게임의 모든 활성 아이템 수라고 단정하지 않는다.
- 전체 타입 대조는 완료했으나 모든 전투 이벤트·상태값 조합을 실기로 검증한 것은 아니다.
  특히 마법 프리팹, 소환물, 상태 효과의 실제 전투 효율은 별도 검증 대상이다.
- 원본 게임 코드·DLL·카탈로그는 로컬에서만 사용한다. 전체 목록에는 식별자·요약만 기록했다.

소스 근거는 `ItemCatalog.LoadCharms/LoadStatMeasurement/LoadCombos`,
`CharmWorth.Resolve`, `PlacementSolver.ScoreOf`, `PositionalWorth`, `ComboCounting`,
`PriorityComboPlacement`, `Worth.OfComboStep`, `CharmCriteria`, `TabletSimulator`다.
게임 어셈블리와 입력 파일의 SHA-256은 전체 목록에 있다.

## 판정 기준

| 판정 | 뜻 |
|---|---|
| 규칙 반영 | 해당 위치·활성·수량 규칙을 모델링함. 전투 효율 정확도와 별개 |
| 부분 반영 | 기본 능력치 또는 관계 일부만 있음. 추가 효과·조건·대상 가치가 빠짐 |
| 효과 미반영 | 별도 동작 계산이 없고 희귀도나 정적 표로 대신함 |
| 추가 검증 | 게임 코드에서 가능성을 확인했지만 실제 프리팹·상태가 없어 결론 유보 |

저장 표 기준 105종은 정적 표, 52종은 정적 표와 추정, 142종은 추정 경로다.
이는 기존 환산 검사와 같은 분류이며 현재 모래시계 연결 모델 등은 별도로 봐야 한다.
정적 표가 있는 아이템도 다른 능력치와의 전환·전투 이벤트까지 완전히 지원된 것은 아니다.

## 배치를 직접 바꾸는 관계

| 항목 | 게임의 근거 | 현재 판정과 영향 | 처리 |
|---|---|---|---|
| 레이에 별조각 (1069) | `Charm_ReduceMPCost.SearchMagic`: 바로 왼쪽 `Charm_Magic`의 `AdditionalCost` 변경. 활성 중 별도 커스텀 능력치도 추가 | 효과 미반영. 마법 옆과 빈자리의 차이, 마나 패널티와의 결합이 점수에 없음 | 우선 수정. 모래시계용 자료를 복사하지 말고 방향·대상·효과 종류를 분리한 연결 모델 도입 |
| 빛나는 모래시계 (1168) | `Charm_RightSpellCooldownHelper.SearchMagic`, `Charm_Magic`의 회복 속도 적용 | 연결과 사용 유지 반영. 추가 버프·마나·시전 빈도를 제외한 사용 빈도 추정 | 기존 회귀 유지. 레이에 별조각과 공통 연결 판정으로 통합하되 회복과 비용 계산은 구별 |
| 하얀 종이 (1258)와 침 (1289/1290) | `Charm_WhitePaper.SearchCategory`가 양옆의 `GetItemCategory()`를 읽음. 침은 사슬 끝의 카테고리를 반환 | 종이에서 침의 정적 정의만 읽어 상속 카테고리 누락 | 이번 작업에서 수정. 직접 침·침 사슬·대상 제거 재현 테스트 |
| 종이와 종이 | 종이의 카테고리도 동적으로 바뀜. `GridInventory.ReleasePermission`은 `Order`로 정렬한 뒤 순차 갱신 | 부분 반영. 현재 정적 카테고리 조회는 종이 중첩과 같은 순서 의존성을 해결하지 않음 | 프리팹 중복 허용과 서버 갱신 순서 확인 후 별도 재현. 임의의 고정점 반복으로 게임 규칙을 대체하지 않음 |
| 북향의 침 | `IsDependencyValid`, `SearchCategory`, `OnRequestCharmDamageBonus` | 대상·사슬·희귀도 추가 조건은 있음. 실제 대상 피해가 아닌 자기 추정값을 사용하며 마법의 공격 가능성을 정의 단계에서 낙관적으로 취급 | 대상 인스턴스의 공격 가능 정보와 연결 효과를 분리. 콤보 상속에는 활성 조건을 임의로 추가하지 않음 |
| 캘세더니 열쇠 | `Charm_3Elemental_ByRow.SearchCategory`는 행에 따라 카테고리와 추가 능력치 종류도 바꿈 | 행 콤보 반영, `addElementalStatByLevel`에 따른 추가 능력치 변화는 누락 | 프리팹 추가 표 수집 후 행별 능력치와 콤보를 같은 배치 상태에서 평가 |
| 거대 망원경 (1108) | `Charm_PlanetModule.SearchPlanet`: 이웃 8칸, 원본 `PLANET` 카테고리와 `Charm_SummonGreenBat` 타입을 함께 요구 | 부분 반영. 카테고리만 검사하며 비활성 대상에도 강화 가치를 줄 수 있음. 크기는 레벨 한 단계 추정 | 대상 타입·실제 사용 가능 상태·비중첩 강화 상태를 구별. 게임의 연결 플래그와 실제 이득은 분리 |
| 헌신의 휘장 (1264) | `Charm_CompanionChaos.SearchCompanion`: 같은 행의 `ICompanionCharm`에 혼돈 모드 설정 | 행·인터페이스 반영, 비활성 동료에도 추정 이득을 줄 수 있음 | 위와 같은 공통 대상 상태 판정. 동료별 혼돈 효과의 수치는 별도 검증 |
| 조화의 수정 | `Charm_NearLevelDamage.UpdateDamageBonus`: 이웃 표시 레벨을 상한만 자른 뒤 계수 곱과 내림 | 부호·상한·내림·양수 가중치/음수 패널티 반영 | 정상 경로 유지. 비활성 이웃의 표시 레벨도 게임이 읽으므로 공통 활성 필터를 붙이면 안 됨 |
| 다용도 벨트 (1030) | `Charm_WoodenBox.CheckQuickSlot`: 앞 여섯 칸의 아티팩트 수로 속성 피해 증가 | 효과 미반영. 앞줄에 아티팩트를 모으는 이득을 평가하지 않음 | 수량 의존 능력치 모델에 포함. 효과원 자체의 레벨·활성과 앞줄 점유를 함께 재계산 |
| 검은 비늘 (1091) | `Charm_CriticalChanceIncreaseWithTablets`: 석판 개수 × 레벨별 치명타 수치 | 효과 미반영. 석판 빼기·교체·합성의 간접 손익도 누락 | 같은 수량 의존 모델로 처리. 합성 후 줄어드는 석판 개수까지 검증 |

게임이 대상을 찾는 조건과 그 대상이 실제로 이득을 내는 조건은 다르다. 조화의 수정과 콤보
복사는 비활성 상태에서도 관계가 남을 수 있으므로, 모든 연결에 같은 활성 필터를 쓰면 안 된다.

## 능력치 전환과 전투 상호작용

| 유형 | 확인한 사례와 근거 | 현재 한계 | 필요한 입력 |
|---|---|---|---|
| 능력치가 다른 능력치로 전환 | 방패 가방 (1053), 빨간 버섯 (1305), 아카데미 만년필 (1291): `Charm_AddStatByDefense`, `Charm_AddStatByAnotherStat` | 기본 `stats`만 수집하고 별도 전환 공식은 미반영. 다른 아이템의 레벨 변경이 전환 결과에 미치는 영향 누락 | 원본 능력치, 레벨별 구간/계수, 대상 능력치, 자체 기여분 제거와 적용 순서 |
| 마법 종류에 따른 강화·패널티 | 이각수 관 (1062): `Charm_BoltMagicMultiShot.CheckInventory/OnCreateMagic` | 볼트 대상의 비용 증가·다중 발사·발사별 피해 변화 미반영 | 실제 `ContainedMagic` 종류, 비용, 발사 수·비율, 사용 가능 상태 |
| 마법·소환물·동료 | `Charm_Magic`, `Charm_SummonGreenBat`, `Charm_SummonUnit`, `Charm_LeadNPC` | 희귀도/정적 표는 실제 피해·행동·지속시간을 대체하지 못함 | 프리팹 수치와 관련 능력치, 행동 주기, 발동 조건 |
| 피격·가드·회피·마나 소비·층 이동 | 전체 목록의 이벤트 구독/주기 갱신 클래스 | 발동 빈도, 중첩, 시간 경과, 방어/자원과의 결합이 대부분 모델 밖 | 시간창 또는 명시적 시나리오. 임의 빈도를 실제 DPS로 표시하지 않음 |
| 상태 효과의 해금·변형 | 환산 불가 능력치가 있는 `Charm_StatusInstance` 포함 | 전환/해금의 값이 숫자로 수집돼도 환산율이 없거나 하위 동작이 빠질 수 있음 | `StatusInstance`와 실제 소비 지점까지 연결한 검증 |

이 그룹은 상수 가중치를 더 붙이는 것으로 해결되지 않는다. 배치별 최종 효과를 모으는 구조가
먼저 필요하며, 정적 배치 효과와 전투 시나리오를 구분해야 한다. 전투 효과 136개가 모두
검증됐다는 의미가 아니라, 136개 동작을 빠짐없이 목록에 올리고 현재 처리 경로와 연결한 것이다.

## 콤보

1. `Worth.OfComboStep`은 모든 콤보에 발동 3.4, 진행 0.43을 공통 적용한다. 측정된 콤보별
   능력치를 배치마다 합산하지 않는다. 측정 도구의 존재와 런타임 반영을 구분해야 한다.
2. `ItemCatalog.LoadCombos`는 `addStatByCombo`와 `setStatus`만 발동 단계에 넣는다.
   `ComboEffect_FlameSword`, `ComboEffect_Debuff`, `ComboEffect_Mystic`,
   `ComboEffect_Alchemy`, `ComboEffect_Guardian`, `ComboEffect_DarkCloud`가
   `OnRequestComboData`에서 직접 추가하는 단계는 합치지 않는다. 저장 카탈로그의
   `FLAMESWORD`는 4/6/8/10만 있다. 실제 프리팹의 특수 단계 값은 재수집으로 확정해야 하며,
   클래스 생성자의 기본값을 프리팹의 실제 수치로 단정하면 안 된다.
3. 해결은 특수 클래스마다 숫자를 복사하는 것이 아니라, 게임의 공통 콤보 데이터 경로에서
   발동 단계를 수집하는 것이다. 효과 설명 번역 성공 여부와 단계 수집을 결합하지 않아야 한다.
   `RequestComboData`가 런타임 상태를 요구하는 경우는 오류를 알리고 불완전한 카탈로그로 표시한다.
4. 신비 각인은 별도 규칙과 동기화된 좌표를 읽는 모델이 있다. 이 부분을 콤보 전투 효과 전체가
   지원됐다는 뜻으로 확대하면 안 된다.
5. 동적 카테고리는 기본 정의에 더하는 것인지 대체하는 것인지도 구별해야 한다. 침과 종이는
   `GetItemCategory`를 대체한다. 저장 프리팹의 정적 카테고리가 비어 있는 사례만으로 일반화를
   보장할 수 없다. 유니크 페어가 더한 개수와 제거 후보의 개수 변화도 후속 대조 대상이다.
6. F2 우선 콤보 탐색은 열쇠·종이를 대상으로 한다. 이번 종이 채점 수정이 침을 함께 옮기는
   전용 탐색까지 추가한 것은 아니다. 판정·평가·탐색의 지원 범위를 따로 표시해야 한다.

## 석판과 활성 조건

- 석판 68종 중 67종은 정적 쿼리, 1종은 사용자 쿼리다. 사용자 쿼리는 `GameReader`가
  인스턴스에서 읽는다. 각 석판의 쿼리·조건을 전체 목록에 기록했다.
- 게임 `StoneTablet`의 레벨·비활성·조건 무시·배수 효과는 `TabletSimulator`가 처리한다.
  현재 저장 카탈로그에는 과거 비교 35,616건, 불일치 0건이 기록돼 있다. 이는 이번에 게임을
  실행해 다시 측정한 결과가 아니다. 현재 입력에서도 동등한지 재수집 시 재검증해야 한다.
- 배수 누적과 인챈트 적용 순서는 기존 포팅 경로를 유지한다. 석판 수에 반응하는 아티팩트와
  신비 같은 외부 각인은 쿼리 파서만 검증해서는 확인되지 않는다.
- 배치 조건 9종은 현재 게임 `GetCriteria`와 코드 대조에서 차이를 찾지 못했다. `FullHP`는
  전투 상태 조건이므로 현재 항상 만족으로 취급한다. 사용 유지가 체력 만점을 보장하는 것은 아니다.
- 미지의 조건 이름은 현재 `None`으로 처리된다. 향후 게임 조건 추가를 자동으로 정상 지원으로
  오인하지 않도록 카탈로그의 미지원 조건 진단이 필요하다.

## 이번 수정과 검증

종이와 침의 카테고리 연결을 회귀 테스트로 먼저 재현했다. 직접 침과 침 사슬 두 사례 모두
수정 전에는 예상 `EMBER` 대신 빈 목록으로 실패했다. `PositionalWorth.CategoriesOf`가
실제 이웃 배치를 받을 때 침의 최종 대상 카테고리를 반환하도록 바꿨고, 종이의 양옆 판정이
그 경로를 사용하도록 연결했다. 대상이 사라지면 종이의 카테고리도 사라지는 경우를 함께 검사한다.

`scripts/check.ps1`에서 테스트 382개(실패·건너뜀 0개), 포맷, DataTool·Plugin 빌드가
통과했다. 빌드는 경고·오류 0개다. 실제 게임 화면과
멀티플레이에서 이번 변경의 동작은 아직 확인하지 않았다. 이번 점검은 0.2.5 배포물을 변경하지 않는다.

## 다음 수정 순서와 완료 조건

| 순서 | 작업 | 완료 조건 |
|---|---|---|
| 1 | 특수 콤보 단계와 효과 지원 범위 수집 | 게임 공통 데이터의 단계와 일치, 번역 실패에도 단계 누락 없음, 미지원 유형이 명시됨 |
| 2 | 방향·대상·효과 종류를 분리한 연결 모델 | 모래시계·별조각·침의 대상 없음/비활성/종류 불일치/사슬/연결 교체 검증. 수치 추정과 구조 판정 분리 |
| 3 | 배치별 수량·능력치 전환 | 벨트·비늘·열쇠·전환 3종이 같은 배치 상태를 사용. 빼기·교체·합성 전후도 재계산 |
| 4 | 카테고리와 관계의 연쇄 | 종이 중첩, 침+종이, 동적 카테고리 대체, 유니크 페어를 게임 갱신 순서와 대조 |
| 5 | 목적별 전투 평가 | 위 누락이 해결된 범위에서만 화력/생존/자원 비교. 입력이 없는 효과는 추정으로 유지 |

각 단계는 작은 가방의 가능한 배치를 열거한 기준 결과와 비교하고, 실제 F10 스냅샷을
반복 재계산하여 적용 후 흔들림을 확인한다. 테스트 통과와 실기 검증을 별도로 기록한다.

## 목록 재생성

```powershell
ilspycmd 'D:/SteamLibrary/steamapps/common/Sephiria/Sephiria_Data/Managed/Assembly-CSharp.dll' -o artifacts/effect-audit/decompiled
python scripts/audit-effects.py --catalog "$env:LOCALAPPDATA/SephPlanner/catalog-generations/20260907T103854027-38738e3fc000497faf30efd6eaadbf25" --decompiled artifacts/effect-audit/decompiled/Assembly-CSharp.decompiled.cs --assembly 'D:/SteamLibrary/steamapps/common/Sephiria/Sephiria_Data/Managed/Assembly-CSharp.dll' --output docs/EFFECT-AUDIT-INVENTORY-2026-09-09.md
```

게임 갱신 후에는 새로 수집한 카탈로그로 경로를 바꾸고 판정 문서도 다시 검토한다. 자동 목록은
수동 판정을 대신하지 않는다. 스크립트는 중복 엔티티와 카탈로그 동작 타입 누락을 오류로 처리한다.
